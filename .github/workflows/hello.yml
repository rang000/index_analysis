name: Chapter3
on: push
env: 
  FORMAT_FUNC: ${{format('{0}, {1}.', 'Hi', 'world')}}
jobs: 
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world"
      - uses: actions/checkout@v4
      - run: printenv 